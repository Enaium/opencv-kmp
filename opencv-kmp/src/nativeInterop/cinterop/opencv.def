package = cvk
headers = opencv_kmp.h
headerFilter = opencv_kmp.h

# Every OpenCV/3rdparty static archive is embedded into the klib via
# separate -libraryPath/-staticLibrary options from build.gradle.kts.
# Consumers additionally need these system libraries; -lstdc++ is required
# because the static OpenCV objects are compiled with GCC libstdc++.
linkerOpts.linux = -lm -ldl -lpthread -lstdc++
linkerOpts.mingw = -lole32
linkerOpts.android = -lm -ldl
